
import 'package:flutter/material.dart';

class AdminEmployeeManagement extends StatelessWidget {
  const AdminEmployeeManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin Employee Management'),
    );
  }
}
